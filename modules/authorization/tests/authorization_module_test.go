package tests

import "testing"

// TestAuthorizationModuleCompiles is a smoke test that verifies the module
// compiles and the package is importable. Real integration tests require a
// running Postgres instance and are deferred to Phase 7.
func TestAuthorizationModuleCompiles(t *testing.T) {
	t.Log("authorization module compiles")
}
